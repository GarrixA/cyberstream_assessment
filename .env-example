PORT=3001

SERVER_URL=http://localhost:8081
DEPLOYED_URL=https://cyberstream.onrender.com
CLIENT_URL=http://localhost:3000

DB_DEV_URL=postgres://<user>:<password>@localhost:5432/cyberstream
DB_TEST_URL=postgres://<user>:<password>@localhost:5432/cyberstream_test
DB_PROD_URL=
DEV_MODE=development
DB_HOSTED_MODE=local
# For Railway/production: set DB_HOSTED_MODE=remote, DEV_MODE=production, and DB_PROD_URL to your Postgres URL.

SESSION_SECRET=your-session-secret
ACCESS_TOKEN_SECRET=your-access-token-secret

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Attendance overtime demo settings
ATTENDANCE_MAX_HOURS=0
ATTENDANCE_MAX_MINUTES=0
ATTENDANCE_MAX_SECONDS=30
ATTENDANCE_CHECK_INTERVAL_MS=10000
